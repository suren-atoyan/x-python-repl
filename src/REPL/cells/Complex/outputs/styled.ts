import { Stack } from '../../../styled';

import styled from '@emotion/styled';

const OutputComponentContainer = styled(Stack)({
  paddingBottom: 8,
  paddingTop: 3,
  width: 'fit-content',
});

export { OutputComponentContainer };
